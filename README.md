# infinite-streaming-cli
* clone repo
* cd directory
* `npm install`
* install dependencies for https://www.npmjs.com/package/node-record-lpcm16
* `export GOOGLE_APPLICATION_CREDENTIALS=serviceaccount.json`
* `node index.js`
